# Example environment variables for local development
# Copy this file to .dev.vars and replace with your actual values.

# Internal auth key used by createInternalAuthMiddleware()
# Must match the value in wrangler.jsonc vars.INTERNAL_KEY_BINDING
INTERNAL_KEY_BINDING=your_internal_service_key_here

# Note: .dev.vars is ignored by git to prevent committing sensitive information.
