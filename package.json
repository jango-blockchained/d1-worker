{
  "name": "d1-worker",
  "version": "1.0.0",
  "description": "HOOX data-plane worker: D1 SQL proxy with table allowlist and keyword firewall, settings KV API, balances, positions, and system logs.",
  "license": "Apache-2.0",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "@hoox-sh/hoox-shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3",
    "wrangler": "^4.95.0"
  },
  "author": "jango-blockchained (hoox-sh)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hoox-sh/d1-worker.git"
  }
}
